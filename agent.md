# H5 App Demo 开发指导

## 总目标

本项目用于制作一个完整 App 的 H5 演示原型。最终交付以 `index.html` 为主，要求页面视觉尽量做到与用户提供的截图一比一还原，同时具备基础可点击、可跳转、可输入、可弹窗的交互体验。

默认使用简体中文与用户沟通，除非用户明确要求使用其他语言。

## 核心要求

1. 所有页面必须优先追求 UI 一比一还原。
2. 不接真实接口，不写后端服务。
3. 所有业务数据、列表数据、用户信息、状态结果都使用前端模拟数据。
4. 页面需要可交互，不能只是静态截图。
5. 后续用户会一张一张提供页面截图，每次都在同一个 `index.html` 中继续追加或调整。
6. 保持单文件 H5 方向，除非用户明确要求拆分工程。

## 文件方向

- 主文件：`index.html`
- 样式：优先内联写在 `<style>` 中。
- 脚本：优先内联写在 `<script>` 中。
- 图片：优先复用 `image/` 目录已有资源；如截图需要新资源，再按页面需求补充。
- 不引入构建工具，不依赖 npm，不接接口。

## 页面还原标准

每一页都应以用户截图为准，重点还原：

- 屏幕尺寸和移动端画布比例。
- 状态栏、导航栏、底部栏等 App 外壳。
- 背景色、渐变、图片、图标、阴影。
- 字体大小、字重、颜色、行高。
- 元素间距、圆角、边框、透明度。
- 按钮、输入框、列表、卡片、弹窗等组件状态。
- 空状态、禁用状态、选中状态、加载状态等视觉细节。

如果截图中包含模拟器窗口标题栏，默认只还原 App 屏幕内容；除非用户明确要求连模拟器标题栏一起还原。

## 交互标准

每个页面不能只做成静态展示，应尽量补齐自然交互：

- 输入框可以输入。
- 密码框可以显示/隐藏密码。
- 按钮可以点击，并给出合理反馈。
- Tab、菜单、筛选、开关、勾选可以切换状态。
- 列表项、卡片、入口按钮可以跳转到对应模拟页面。
- 弹窗、抽屉、Toast、确认框可打开和关闭。
- 登录、注册、提交、保存等动作使用模拟结果。

交互逻辑只服务演示，不做真实鉴权、真实校验或真实网络请求。

## 路由与页面组织

建议使用 hash 路由管理页面，例如：

- `#/login`
- `#/home`
- `#/profile`
- `#/settings`

每一页使用独立渲染函数或独立页面容器，方便后续逐页追加。

页面跳转应模拟真实 App 流程。用户提供新截图时，应根据截图内容判断它属于新页面、弹窗、状态变化，还是当前页面的交互结果。

## 模拟数据原则

模拟数据应写在 JS 常量中，命名清晰，便于后续替换：

- 用户资料。
- 家庭成员。
- 设备列表。
- 聊天消息。
- 商品或服务信息。
- 订单、记录、通知、设置项等。

模拟数据要贴合截图内容，不要生成与截图无关的大段演示文案。

## 视觉实现原则

- 移动端优先，默认按截图宽度进行精确还原。
- 优先使用 CSS 还原布局和状态。
- 图标优先复用现有图片资源；简单图标可以用 CSS 或内联 SVG。
- 不做营销落地页，不加与截图无关的装饰。
- 不随意改变用户截图中的配色、文案、层级和布局。
- 不为了“好看”擅自重设计页面。

## 验收方式

每完成一页或一次明显改动后，应尽量进行本地浏览器预览检查：

1. 打开 `index.html` 或本地静态服务地址。
2. 按截图对应移动端宽高检查布局。
3. 检查是否有控制台错误。
4. 检查主要交互是否可用。
5. 对照用户截图微调位置、颜色、字号和间距。

## 沟通方式

当用户提供页面截图时，直接开始实现。除非缺少关键信息导致无法判断页面流程，否则不要频繁追问。

完成后用简短中文说明：

- 改了哪个文件。
- 新增或完成了哪个页面。
- 可点击或可交互的部分。
- 本地预览地址或打开方式。
